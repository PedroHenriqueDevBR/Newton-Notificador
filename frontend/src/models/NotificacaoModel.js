class Notificacao {
  constructor(id, titulo, descricao, sistema, lista_status, destinatarios) {
    this.id = id
    this.titulo = titulo
    this.descricao = descricao
    this.sistema = sistema
    this.lista_status = lista_status
    this.destinatarios = destinatarios
  }
}

export default Notificacao
